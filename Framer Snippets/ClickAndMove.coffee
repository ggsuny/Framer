plugin.run = (contents, options) ->
	"""
#{contents}
leng = Math.pow((event.pageX - iMouseDownX),2)+Math.pow((event.pageY - iMouseDownY),2)
	if leng > 00000 # As a movement 
	
	else # As a Click

	"""